// import { actionTypes } from "../actions/actionTypes";

// export default (state = [], action) => {
//   switch (action.type) {
//     case actionTypes.ADD_BOOKMARK:
//       return {
//         ...state,
//         postFeeds: state.postFeeds.map(bookmark =>
//           bookmark.id === action.id ? {...bookmark} : bookmark,
//         ),
//       };
    
//     default:
//       return state;
//   }
// };